AS-BUILT: Interactive Keynote Proposal Automation
==================================================
Status: reflects the deployed pipeline as of 2026-08-17. This is an
independent project — it shares no code, credentials file, or database
tables with the Uncharted Ice automation
(C:\Users\Cloverly\claude_code\sales_proposals_automation), though it was
built from that project as an architectural blueprint and currently reuses
the same Google Workspace identity (julienne@myadventuregroup.com.au) and
the same Supabase project ("MAGTestProject", aivitcomiywiysrfwqxt) for
convenience. Treat divergence between this file and the code as a bug in
this file.

1. PURPOSE
----------
Auto-generates a customized Interactive Keynote sales proposal (James
Castrission's "Crossing the Ice" keynote) in Google Slides from
handwritten/typed demo-call notes received via email, using a single
master template stored in Google Drive, with a Supabase audit log and
pre-send QA flags. Mirrors Uncharted Ice's proven pipeline shape, adapted
for a single-offering speaker product rather than a multi-template
service catalog.

2. ARCHITECTURE & COMPONENTS
-----------------------------
Stack: Python 3.10+, same library set as Uncharted Ice:
  google-api-python-client, google-auth, google-auth-oauthlib, anthropic,
  supabase, python-dotenv, requests, plus python-pptx (used only by
  build_master_template.py, not by the runtime pipeline).

Integrations:
  - Email: Gmail API (OAuth2 refresh-token flow, own .env)
  - OCR/Entity Extraction: Anthropic Claude API, model claude-opus-5, vision
  - Storage: Google Drive API
  - Presentation: Google Slides API
  - Dedup/Audit: Supabase (Postgres via supabase-py)
  - Optional enrichment: Fathom API (call recordings) — same optional,
    best-effort pattern as Uncharted Ice

Entry point: main.py -> pipeline.run_once() — safe to invoke repeatedly
(dedup via Supabase makes reruns idempotent).

File/module map:
  - main.py                    — entry point
  - pipeline.py                — orchestration + pre-send QA check
  - config.py                  — env + project_vars.txt loader
  - oauth_setup.py             — one-time Google OAuth consent flow
  - inspect_template.py        — debug CLI, dumps shape/image alt-text
  - build_master_template.py   — builds + (optionally) uploads the master
                                  .pptx template; NOT part of the runtime
                                  pipeline, run manually when the template
                                  needs to be (re)built
  - src/google_clients.py      — Gmail/Drive/Slides client builder
  - src/gmail_service.py       — search/download/send/reply
  - src/ocr_service.py         — Claude vision OCR/extraction
  - src/drive_service.py       — folder create-or-reuse, upload, master
                                  template lookup, duplicate
  - src/slides_rewriter.py     — Claude rewrites slide text + swaps logo
  - src/logo_service.py        — guesses client logo URL, no network call
  - src/fathom_service.py      — optional Fathom call-recording match
  - src/supabase_service.py    — dedup/audit log
  - db/001_create_interactive_keynote_proposal_logs.sql — table migration
  - templates/interactive_keynote_master_template.pptx  — built template
    source (the live Drive copy is the one the pipeline actually uses)
  - .claude/skills/run-interactive-keynote-pipeline/SKILL.md — local run

Deployment: this project lives at
https://github.com/yen93/interactive_keynote_proposals_automation (pushed
as its own independent git repo — no shared history with Uncharted Ice).
A scheduled cloud routine "interactive-keynote-proposals-automation-hourly"
(id trig_01KSh5tdtUWA1BXND9eNgR4R, hourly cron at :12, offset from
Uncharted Ice's :42 so both can share the inbox without firing in the same
minute) has been created and mirrors Uncharted Ice's routine mechanics
(clones the repo fresh each run, writes its own .env from credentials
embedded in its stored job-config prompt, pip installs, runs
`python main.py`). It is currently DISABLED (enabled=false) — created
paused, deliberately not live, per explicit instruction. Enable it via the
routine's page at https://claude.ai/code/routines or the /schedule skill
when ready to go live. Until then, runs on demand only, via `python main.py`
or the run-interactive-keynote-pipeline skill.

3. DIFFERENCES FROM THE UNCHARTED ICE BLUEPRINT (BY DESIGN)
-------------------------------------------------------------
  - No template-selector step. Uncharted Ice's src/template_selector.py
    (LLM picks among several candidate decks) has no equivalent here —
    there is exactly one Interactive Keynote offering/template right now.
    src/drive_service.get_master_template() simply requires the templates
    folder to contain exactly one file with "template" in its name and
    raises if that's ever not true (ambiguity stops the run rather than
    silently guessing).
  - No sibling-routine-chaining step (Uncharted Ice's undocumented-in-repo
    "Step K", which fires a separate runsheet-automation routine). No
    equivalent downstream automation exists for Interactive Keynote.
  - OCR extraction fields are Interactive-Keynote-specific: client_org,
    contact_name, event_date, location, audience_size, duration, summary,
    context, raw_transcript, unclear_fields — no recommended_service field,
    since there's only one offering to recommend. duration defaults to
    "60 minutes" (the standard package) rather than being flagged unclear
    when not mentioned on the call.
  - The master template is not a hand-built recreation — it is a direct
    copy of a REAL, previously-used Interactive Keynote deck ("INTERACTIVE
    KEYNOTE (CAS X PELLO)", Drive file 1BnT1b5G5m1gOXI7UcJrbUdfaQvCNlC-
    Kcp9BAkp0Jt8, supplied directly by the user), not a token-ized blank
    template. Investigation (thumbnail rendering + per-image content
    hashing across all 8 slides) found its actual personalization surface
    is much narrower than a bracket-token design would suggest: exactly
    ONE editable text shape (slide 2's bio paragraph) carries a leftover
    client-organisation mention ("...at your next HMW Group event"), and
    the client logo (a "Pello" wordmark, confirmed by hash to be the exact
    same image asset) recurs in exactly 5 image elements across 5 slides
    (cover + 4 content slides), always paired with a separate "My Adventure
    Group" watermark image. A visually-adjacent "Duration: 60 MINS /
    Audience Size: TBD / Click Play To Watch Showreel" info box turned out
    to be a single flattened image, not editable text — so it is correctly
    left untouched as a "pic," per the instruction that everything besides
    the client name and logo stays exactly the same. The slide-rewrite
    prompt was rewritten to match this reality: find and replace only the
    one client-name mention (minimal wording change, no restructuring),
    return every other shape byte-for-byte unchanged, and never touch the
    cover title (it doesn't name a client in this deck). This supersedes an
    earlier, bracket-token-driven design (still preserved as a documented
    fallback in build_master_template.py) that assumed a blanker,
    more heavily tokenized template — that assumption didn't match the
    real deck once it was actually inspected.
  - Supabase table interactive_keynote_proposal_logs adds three columns
    beyond Uncharted Ice's proposal_demo_notes_email_logs shape:
    client_org, event_date, logo_replaced — for easier operational
    querying without needing to open each Slides deck.
  - GMAIL_TRIGGER_QUERY default is a distinct query so this pipeline and
    Uncharted Ice's can share one inbox without cross-triggering.

4. WORKFLOW (STEP BY STEP)
----------------------------
Trigger: Gmail search (config.GMAIL_TRIGGER_QUERY) + Supabase dedup check
against interactive_keynote_proposal_logs.message_id.

  A. OCR/extraction (src/ocr_service.py) — downloads the attachment, sends
     to claude-opus-5 vision via a forced tool call
     "extract_keynote_demo_notes". Required fields: client_org, summary
     (config.REQUIRED_OCR_FIELDS). Missing/unclear-flagged required fields
     -> run stops for that email, error reply sent, Supabase logged
     status="error".
     CAVEAT (inherited unchanged from the Uncharted Ice blueprint): this
     OCR call happens BEFORE the "started" notification thread is created,
     so an unhandled exception here (e.g. an Anthropic API error) is not
     caught — it propagates out of process_email() entirely. No error
     email is sent and no Supabase row is written for that message, since
     neither the thread nor the try/except block exist yet at that point.
     Confirmed live during testing (see section 9) when the copied
     Anthropic API key had zero credit balance. A future improvement would
     wrap the download+OCR call in its own try/except so at least a
     bare-bones error notification goes out.
  B. "Started" Gmail notification — new thread, subject
     "Automation: Interactive Keynote Proposal for [client_org]".
  C. Fathom call-recording match (src/fathom_service.py) — optional,
     best-effort, identical mechanism to Uncharted Ice; skipped entirely
     if FATHOM_API_KEY is unset.
  D. Master template lookup (src/drive_service.get_master_template) —
     finds the single file with "template" in its name in the Templates
     Drive folder; raises if none or more than one exist.
  E. Client folder + demo-notes upload (src/drive_service.py) — creates or
     reuses "[client_org] [year]" under the master proposals folder;
     uploads the original attachment as
     "[client_org] Demo Call Notes.[ext]".
  F. Template duplication — copies the master template into the client's
     folder as "[client_org] Interactive Keynote Proposal".
  G. Slide text rewrite (src/slides_rewriter.py) — forced tool call
     "rewrite_slide_text" to claude-opus-5; sent as its own batchUpdate.
     Preserves bullet formatting (reapplied post delete/insert, since
     Slides silently drops it otherwise) and shrinks font size (down to
     0.7x) when new text runs meaningfully longer than the original,
     since the Slides API has no working autofit. Shapes >1.5x original
     length flagged as overflow_risk_ids for the QA step.
  H. Logo placement (src/logo_service.py + slides_rewriter.py) — isolated
     batchUpdate, deliberately separate from G so a bad/unverified logo
     guess can never roll back a good text rewrite (a Slides batchUpdate
     is all-or-nothing). Logo domain is guessed from client_org (strip
     legal suffixes, slugify, try .com.au/.com/.co) and handed to Slides'
     replaceImage as an unvalidated Google-favicon URL — this process
     never fetches it itself, exactly like Uncharted Ice, for the same
     reason (see Uncharted Ice's AS_BUILT "lessons learned": self-
     validating a guessed domain breaks under network-restricted sandboxes
     because the validating host either dies or redirects unpredictably).
  I. Pre-send QA check (pipeline.py) — never blocks sending. Flags: no
     logo guessable; guessed logo couldn't be placed; OR (on every
     successful placement) "logo was auto-guessed — verify it's actually
     [client]'s logo"; plus an overflow-risk count if any. Any qa_notes ->
     Supabase status="needs_review", else "success".
  J. Final notification + Supabase upsert — reply in the same Gmail
     thread from step B with the proposal link, an "FYI" block for any
     Fathom/logo matches, and a QA block if qa_notes exist. Upserts
     interactive_keynote_proposal_logs on message_id: is_processed=true,
     status, error_message, proposal_link, processed_at, client_org,
     event_date, logo_replaced.

Error handling: any other unhandled exception inside the try block (i.e.
after the "started" notification exists) replies in-thread with the error
text, logs status="error", is_processed=true. Never auto-retried — a
failed or needs-review email requires a human to intervene (rerun after
fixing template tagging, clear is_processed in Supabase to force a retry,
etc.).

5. DATABASE
------------
Table: public.interactive_keynote_proposal_logs, Supabase project
aivitcomiywiysrfwqxt ("MAGTestProject") — the same shared project used by
Uncharted Ice and several other of this team's automations. See
db/001_create_interactive_keynote_proposal_logs.sql for the exact DDL.

  id              uuid primary key default gen_random_uuid()
  created_at      timestamptz not null default now()
  message_id      text unique          -- Gmail message id; dedup key
  is_processed    boolean not null default false
  status          text                 -- 'success' | 'needs_review' | 'error'
  error_message   text
  proposal_link   text
  processed_at    timestamptz
  client_org      text
  event_date      text
  logo_replaced   boolean not null default false

RLS is enabled on this table (matching the sibling tables in the same
project). No policies have been added — the pipeline connects with the
Supabase service key, which bypasses RLS, so this only matters if the
anon/authenticated roles are ever given access to this table.

NOTE (unrelated pre-existing finding, surfaced during provisioning, not
introduced by this project): the Supabase security advisor flags that six
OTHER tables in this same shared project — public.linkedin_posts,
public.speakers, public.status, public.follow_up_sequence_threads,
public.organization_users, public.gmail_cache — have RLS disabled and are
fully exposed to the anon/authenticated roles. This predates this project
and belongs to other automations sharing the database; it's noted here
only so it doesn't get lost, not something this project's build touched.

6. PROMPTS
-----------
All 3 LLM calls (no template-selection call, unlike Uncharted Ice's 4) use
claude-opus-5 via forced tool_choice calls, embedded as Python strings in
their respective src/*.py modules (not externalized to prompt files, same
convention as Uncharted Ice):

  1. extract_keynote_demo_notes (src/ocr_service.py) — vision input,
     transcribes the demo-notes photo/PDF and extracts client_org,
     contact_name, event_date, location, audience_size, duration, summary,
     context, raw_transcript, unclear_fields. Model is told to default
     duration to "60 minutes" rather than flag it unclear, and to leave
     any other missing/illegible field empty + listed in unclear_fields
     rather than guess.
  2. match_meeting (src/fathom_service.py) — unchanged from Uncharted Ice;
     matches demo notes to a recent Fathom recording by name/contact/date
     proximity, requires >= "medium" confidence.
  3. rewrite_slide_text (src/slides_rewriter.py) — targets the real
     master deck's actual (narrow) personalization surface: find the
     shape(s) mentioning a leftover client/organisation name and replace
     just that name with the new client_org from the demo notes, making
     only the minimal wording change needed for the sentence to still read
     naturally. Every other shape — including the cover title, which does
     not name a client in this deck — must be returned character-for-
     character identical to its original text. No bracket tokens exist in
     the real template; the prompt does not look for them. Length
     constraint (~10-15% of original) and shrink/overflow mechanics are
     kept as a safety net (identical to Uncharted Ice's) but rarely
     trigger in practice, since only one shape's text actually changes.

7. DOCUMENT GENERATION
------------------------
Google Slides API (batchUpdate) + Drive API (copy/folder management),
same as Uncharted Ice — no docx/pdf export step; the deliverable is the
live Slides deck, and the "ready" email carries its edit link.

Master template: the live template is a Drive copy of the REAL "INTERACTIVE
KEYNOTE (CAS X PELLO)" deck the user supplied (source file
1BnT1b5G5m1gOXI7UcJrbUdfaQvCNlC-Kcp9BAkp0Jt8, in the user's own working
Drive folder, left completely untouched), copied into this project's
Templates folder as "Interactive Keynote Template - Crossing the Ice"
(file id 1fO5ZoCsn3LH87BMqc14Vvw9HJcpN-6v3ehR0s45MLws at time of writing).
Copying preserves Slides' internal objectIds, images, fonts, and layout
exactly — this is real photography and real design, not a recreation.

The 5 client-logo image elements (objectIds g3f13f3c02bf_0_1, _0_2, _0_4,
_0_8, _0_12 — found by rendering slide thumbnails, extracting each image's
actual content URL, and comparing SHA-256 hashes across all 8 slides to
identify every occurrence of the exact same "Pello" wordmark asset) were
tagged with alt-text/description "client_logo" via a one-time Slides
`updatePageElementAltText` batchUpdate on the COPY only (the user's
original file was never modified). This matches Uncharted Ice's detection
convention (title/description containing "logo"/"client_logo"/"client
logo") in slides_rewriter.find_logo_placeholders() — confirmed via
inspect_template.py and a direct Drive content read after tagging.

FALLBACK (not currently used): build_master_template.py still exists as a
documented fallback — a python-pptx-generated recreation using bracket
tokens, in case the real Drive template file is ever lost. If it's ever
promoted back to production, slides_rewriter.py's prompt would need to
revert to the bracket-token-driven version (see that script's docstring
for the historical version). Its own known limitation: no PDF renderer was
available to extract the ORIGINAL reference PDF's colors/fonts/imagery, so
that fallback's visual design is a best-effort placeholder only — moot for
the actual production template, which uses the real deck's real assets.

Filename conventions (identical pattern to Uncharted Ice, new wording):
  - Client folder: "[client_org] [year]"
  - Uploaded original notes: "[client_org] Demo Call Notes.[ext]"
  - Generated deck: "[client_org] Interactive Keynote Proposal"
  - Master template file in Drive: "Interactive Keynote Template -
    Crossing the Ice"

8. CONFIGURATION, DEPENDENCIES, SECRETS
------------------------------------------
.env (secrets, gitignored, never committed):
  GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REFRESH_TOKEN
  ANTHROPIC_API_KEY
  SUPABASE_URL, SUPABASE_SERVICE_KEY
  FATHOM_API_KEY (optional)
  GMAIL_TRIGGER_QUERY (optional override)
  FATHOM_LOOKBACK_DAYS (optional, default 30)

project_vars.txt (non-secret, JSON array with one object):
  demo_notes_recipients, notification_recipients (both currently
  ["julienne@myadventuregroup.com.au"]), proj_drive_link,
  templates_drive_link — pointing at newly-created Drive folders:
  "Interactive Keynote Proposals" (master) and its "Templates" subfolder.

requirements.txt: same as Uncharted Ice plus python-pptx (only used by
build_master_template.py, not the runtime pipeline).

CREDENTIAL PROVENANCE: the live .env in this project currently contains
credential VALUES copied from the read-only Uncharted Ice project's .env
(same Google Workspace identity, same Supabase project, same Fathom
account) — done deliberately to enable a genuine end-to-end test without
requiring a fresh interactive OAuth consent flow (which needs a local
browser and can't be completed by an agent). The Uncharted Ice project
itself was never modified. Rotating credentials on either project affects
both, since they currently share the same underlying accounts — treat
this the same operational caveat Uncharted Ice's own AS_BUILT doc already
notes about its shared-identity setup, now doubled across two consumers
of the same keys.

9. TESTING PERFORMED
----------------------
Real, live infrastructure was provisioned and exercised throughout — this
was not a paper design, and the final test used real client data end to
end with real Claude/Fathom/Google/Supabase calls:

STAGE 1 — infrastructure + synthetic smoke test (before the real template
was supplied, and before the Anthropic account had credit):
  - Supabase table interactive_keynote_proposal_logs created for real via
    the Supabase MCP tool in project aivitcomiywiysrfwqxt; verified via
    list_tables.
  - Drive folders "Interactive Keynote Proposals" and its "Templates"
    subfolder created for real.
  - A first-pass (later superseded, see below) master template was built
    via build_master_template.py and uploaded/converted to native Google
    Slides via the Drive API using the pipeline's own OAuth credentials —
    proving the credential/scope setup works before the real template was
    available.
  - A synthetic test email ("Test Co Pty Ltd") was sent and a real
    `python main.py` run found it, but the live OCR call failed with a
    genuine Anthropic 400 "credit balance is too low" — an external
    account state, not a code defect. This also exposed the
    inherited-from-blueprint gap noted in section 4, step A: because the
    failure happened before the "started" notification thread existed,
    the run crashed with no error email/Supabase row for that message.
  - To still validate the rest of the architecture before credit was
    added, a throwaway dry-run harness monkeypatched only the three
    Anthropic-dependent calls and confirmed the whole non-LLM machinery
    (Drive folder/upload/duplicate, Slides batchUpdate, logo replaceImage,
    Gmail notifications, Supabase upsert) worked correctly end to end. All
    artifacts from this synthetic run were cleaned up afterward.

STAGE 2 — real template swap + real end-to-end test (after the user
supplied the actual reference Slides deck and topped up Anthropic credit):
  - The user provided the real, previously-used Interactive Keynote deck
    (Drive file 1BnT1b5G5m1gOXI7UcJrbUdfaQvCNlC-Kcp9BAkp0Jt8). It was
    copied into the Templates folder, its 5 logo image instances were
    identified by thumbnail rendering + per-image SHA-256 hashing and
    tagged "client_logo" (see section 7), and the old python-pptx-based
    template file was retired/trashed from Drive.
  - slides_rewriter.py's rewrite prompt was rewritten to match the real
    deck's actual (narrow) personalization surface — see section 3/6.
  - A real "Demo Call Notes" scan (test_demo_notes_for_interactive_
    keynote.pdf) had appeared in this project's root directory partway
    through the session, for a real prospect ("Whiddon", contact Lauren
    Fairbairn, event "Powered By Purpose," 24/11, Glenfield Sydney,
    100pax, $8,500, "Interactive Keynote" explicitly checked as the
    requested type). This was not created by this build and was
    confirmed with the user before use rather than assumed to be a
    disposable fixture. Per the user's explicit instruction, it was used
    as the real test input.
  - The PDF was emailed to the monitored inbox (via the pipeline's own
    Gmail credentials, subject "Interactive Keynote demo notes -
    Whiddon") and `python main.py` was run for real. First attempt: OCR
    succeeded, Fathom matching found a HIGH-confidence recording match,
    but the Drive copy of the (real, ~19MB, image-heavy) template hit a
    ConnectionResetError after a ~10-minute stall — a transient network
    issue in this environment, not a code defect; logged correctly to
    Supabase as status="error" per the no-auto-retry design. The Supabase
    row was cleared and the run retried.
  - Second attempt succeeded completely, fully live, no mocking:
      * OCR (claude-opus-5) correctly extracted client_org="Whiddon",
        event_date="24/11", and other fields from the real handwritten-
        style intake form.
      * Fathom matched the same high-confidence recording again.
      * The one real client-name mention was correctly and minimally
        changed: "...at your next HMW Group event" became "...at your
        next Whiddon event" — no other wording in that sentence/paragraph
        changed.
      * Every other shape in the deck — cover title/tagline, full bio,
        the "Crossing the Ice" narrative, Learning Outcomes, all three
        testimonials, Conference Gifts, DESIGN/DELIVERY bullets, the
        $8,500 AUD investment line, and the contact block — came back
        byte-for-byte identical to the original, confirmed via a direct
        Drive content read of the generated deck.
      * Logo step guessed domain "whiddon.com.au" and successfully
        replaced all 5 tagged image instances (logo_replaced=true).
        Visual confirmation via a rendered slide-1 thumbnail: the "Pello"
        wordmark was correctly swapped for a (generic, unverified)
        favicon-service icon — exactly the expected, correctly-flagged-
        for-review behavior, not a bug.
      * Supabase row: status="needs_review" (unverified logo guess,
        correctly flagged), client_org="Whiddon", event_date="24/11",
        logo_replaced=true, proposal_link populated, is_processed=true.
      * The generated deck was left in place (not cleaned up) as a real,
        reviewable work product — see the session's final summary for
        what to do with it.

STAGE 3 — deployment infrastructure:
  - This project was pushed to its own new, independent GitHub repo
    (https://github.com/yen93/interactive_keynote_proposals_automation),
    created by the user (gh CLI/GITHUB_TOKEN were not available in this
    environment) and pushed via the existing Windows git credential
    manager. .env, the real Whiddon PDF, and .claude/settings.local.json
    are excluded via .gitignore and were confirmed absent from the
    staged/pushed tree before pushing.
  - A cloud routine ("interactive-keynote-proposals-automation-hourly",
    trig_01KSh5tdtUWA1BXND9eNgR4R) was created mirroring Uncharted Ice's
    routine mechanics, confirmed via RemoteTrigger get to be enabled=false
    (paused, not live) as explicitly instructed.

NOT LIVE-TESTED: nothing of substance remains untested in this pipeline —
OCR, Fathom matching, the LLM slide rewrite, the logo swap, Gmail
notifications, and Supabase logging have all now run for real, against
real client data, producing a reviewable output. What has NOT been tested
is the routine actually firing on its cloud schedule (it's deliberately
paused) and a second/third real client run (to build confidence the
rewrite behaves consistently across different demo-notes inputs, not just
this one).

10. RUN INSTRUCTIONS
-----------------------
1. Ensure .env is populated (see .env.example) — either by running
   `python oauth_setup.py` for fresh Google credentials, or by pointing at
   the credential values already present in .env from this build.
2. `pip install -r requirements.txt`
3. `python main.py` (or ask Claude Code to run the
   run-interactive-keynote-pipeline skill) — scans Gmail, processes every
   unprocessed matching email once, exits.
4. To rebuild/redeploy the master template after a design change:
   `python build_master_template.py --upload` (delete or rename the
   previous Drive template file first — get_master_template() expects
   exactly one match).
5. To inspect a deck's shape IDs / verify logo-tag placement:
   `python inspect_template.py <presentation_id_or_url>`

11. TROUBLESHOOTING
----------------------
- "Expected exactly one Interactive Keynote master template, found N":
  more than one file with "template" in its name exists in the Templates
  Drive folder — remove/rename the extras.
- "No file containing 'template' found...": the master template was
  deleted or never uploaded — rerun build_master_template.py --upload.
- Anthropic 400 "credit balance too low": the account behind
  ANTHROPIC_API_KEY needs topping up (console.anthropic.com) — this
  blocks OCR, Fathom matching, and slide rewriting simultaneously, and
  (per section 4/9) can currently crash a run with no notification/log
  entry if it happens on the very first email of a run.
- A run reports "needs_review": expected and non-blocking — check the
  reason in the Gmail reply / Supabase error_message column (usually an
  unverified logo guess or a text-overflow risk) and fix manually if
  needed.
- The scheduled routine (trig_01KSh5tdtUWA1BXND9eNgR4R) exists but is
  paused — see section 2. Enable it at
  https://claude.ai/code/routines/trig_01KSh5tdtUWA1BXND9eNgR4R (or via the
  /schedule skill) once ready to go live. Before enabling: confirm
  GMAIL_TRIGGER_QUERY actually matches how demo notes arrive in practice
  (still a best guess — see section 12), and decide whether the shared
  Google/Anthropic/Supabase/Fathom credentials with Uncharted Ice are
  acceptable long-term or should be split out first.

12. FUTURE IMPROVEMENTS
--------------------------
- Wrap the initial download+OCR call in pipeline.process_email() in its
  own try/except so an OCR-time failure (e.g. a billing error) still
  produces an error notification and a Supabase row, instead of crashing
  run_once() silently for that email (inherited gap from the Uncharted
  Ice blueprint, not yet fixed here — see section 4).
- Enable the paused routine once the team confirms GMAIL_TRIGGER_QUERY
  matches real inbound demo-notes email traffic (currently a best guess,
  not observed real traffic) and decides on credential independence from
  Uncharted Ice.
- Improve the logo step's hit rate: the Whiddon test showed the favicon-
  guess approach falling back to a generic single-letter icon rather than
  a real logo for a lesser-known domain — acceptable given it's always
  flagged for human review, but a more reliable logo source (e.g. Clearbit
  successor, manual upload step, or asking for a logo URL in the demo
  notes) would reduce how often needs_review fires for this reason alone.
- Run 2-3 more real client scenarios through the pipeline to build
  confidence the single-client-name-mention detection generalizes (e.g. a
  client name that appears in multiple forms/cases, or a demo-notes email
  with genuinely illegible handwriting) before enabling the routine.
- Consider duration-tiered pricing if the business ever wants price to
  vary by requested keynote length — currently the $8,500 figure is
  always preserved verbatim regardless of the requested duration, per the
  real template's own fixed pricing.
